{
    "version": "2.0.0",
    "tasks": [{
        "label": "build",
        "command": "pub run build_runner build", // Could be any other shell command
        "args": ["test"],
        "type": "shell"
    }]
}